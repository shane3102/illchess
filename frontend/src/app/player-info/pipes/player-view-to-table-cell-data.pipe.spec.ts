import { PlayerViewToTableCellDataPipe } from './player-view-to-table-cell-data.pipe';

describe('PlayerViewToTableCellDataPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerViewToTableCellDataPipe();
    expect(pipe).toBeTruthy();
  });
});
